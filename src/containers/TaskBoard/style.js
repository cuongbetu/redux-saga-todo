const styles = (theme) => ({
  taskboard: {
    display: "block",
    textAlign: "center",
    margin: 10,
  },
  shape: {
    backgroundColor: theme.color.primary,
    color: theme.typography.textColor,
    padding: 10,
    margin: 10,
  },
  button: {
    marginRight: 10,
  },
});

export default styles;
