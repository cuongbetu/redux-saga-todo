import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  color: {
    primary: "#0288D1",
    secondary: "#CDDC39",
    error: "#E64A19",
  },
  typography: {
    textColor: "#5d4037",
  },
});

export default theme;
